import { Request, Response } from "express";

class DefaultController {
    async handle( request: Request, response: Response) {
        return response.status(200).json({ message: "Your API is Running!"});
    }
}

export { DefaultController }