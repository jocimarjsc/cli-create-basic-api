import { Router } from "express";
import { DefaultController } from "./controllers/DefaultController";

const router = Router();
const defaultController = new DefaultController();

router.post("/users", defaultController.handle);

export { router };